{
  "name": "web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npx prisma generate && next build",
    "dev": "next dev"
  },
  "dependencies": {
    "@rainbow-me/rainbowkit": "^2.2.7"
  }
}